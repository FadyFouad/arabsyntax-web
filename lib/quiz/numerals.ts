// Re-export the shared Arabic-Indic numeral helper so the quiz renders digits
// (progress ٣ / ١٠, scores, percentages) consistently with the rest of the site.
export { toArabicIndic } from '@/lib/mutun/numerals';
